# Agent.md

This file is kept as a human-readable alias because some teams say “Agent.md”. Codex’s project instruction file should be `AGENTS.md`; keep the actionable instructions there.

Use this project with:

1. `AGENTS.md` for durable Codex instructions.
2. `.codex/agents/*.toml` for project-scoped custom subagents.
3. `prompts/00_master_build_prompt.md` for the parent agent orchestration prompt.
4. `docs/automation_workflow.md` for the full implementation procedure.
